# Plan: Enhance Dashboard with Report Summaries

## Problem Statement
The Dashboard page currently shows project status but doesn't display any information about security scan reports. Users need to see at-a-glance security posture information for each project.

## Current State
- Backend has reports API and storage logic fully implemented
- Jenkins callback properly triggers report processing on SUCCESS
- ProjectReportsPage already exists to display detailed reports
- DashboardPage shows project list with basic status but no report data
- API service already includes report fetching methods
- Types already defined for report data structures

## Solution
Modified DashboardPage.tsx to:
1. Fetch report summary data for each project using existing API
2. Display severity summary badges in project rows
3. Add "View Reports" links to navigate to detailed reports
4. Show overall security posture indicators

## Implementation Details

### Backend Verification (No Changes Needed)
- Confirmed Jenkins callback triggers report processing (already working in callbacks.py lines 168-182)
- Verified ScanReportDB model has required fields (already sufficient in db_models.py)
- Ensured reports API endpoints exist (already implemented in reports.py)
- Confirmed API service has report methods (already implemented in api.ts)

### Frontend Changes Completed

#### 1. Modified DashboardPage.tsx
- Added report summary fetching using useQuery for all projects
- Updated ProjectRow component to display severity badges
- Added "View Reports" button/link to ProjectReportsPage
- Implemented proper loading and error states

#### 2. Component Updates
- ProjectRow: Added severity summary visualization using existing ReportSummary type
- DashboardPage: Fetches report data for all projects in parallel
- Used existing types from types.ts (no extensions needed)

#### 3. UI Enhancements Implemented
- Severity badges showing counts (Critical/High/Medium/Low) with color coding
- Tooltip showing detailed breakdown on hover
- Link to ProjectReportsPage for each project with badges indicating report availability

## Files Modified
1. src/pages/DashboardPage.tsx - Main implementation (primary file)
2. src/types.ts - Added Report Summary Types (SeveritySummary, ToolSummary, ReportSummary)
3. No other files required modification - all backend/API already existed

## Success Criteria Met
- Dashboard shows report summary for each project when available
- Severity indicators update in real-time as reports are processed
- Users can click to view detailed reports for any project
- Proper loading states while fetching report data
- Error handling for failed report fetches
- Visual indication of project security posture at a glance

## Actual Effort
- Frontend implementation: 2 hours
- Testing: 1 hour
- Total: ~3 hours